# Complexity thresholds enforced via the workspace clippy lints (see Cargo.toml).
# These are the structural guard against the prior build's oversized, tangled code.
too-many-lines-threshold = 120
cognitive-complexity-threshold = 25
too-many-arguments-threshold = 7
type-complexity-threshold = 250
